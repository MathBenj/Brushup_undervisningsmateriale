%%%
%%% Preamble
%%% 
\documentclass[11pt]{article}
%%
%% Usual stuff
%%
\usepackage[utf8]{inputenc}
\usepackage[danish]{babel}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{xcolor}
\usepackage{hyperref}
\hypersetup{%
	colorlinks = true,
	linkcolor = blue,
	urlcolor  = blue,
	citecolor = blue,
	anchorcolor = blue,
	%pdfpagelabels=true,%
	plainpages=false,%
	pdfauthor={Author(s)},%
	pdftitle={Title},%
	pdfsubject={Subject},%
	bookmarksnumbered=true,%
	pdfstartview=FitH%
}
\usepackage{ocgx2}
%%
%% Math
%%
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{mathtools}
\usepackage{mathrsfs}
\usepackage{amsthm}
\usepackage{dsfont}
\newcommand{\dd}{\,d}

%%
%% Tikz
%%
\usepackage{tikz}
\usetikzlibrary{patterns,calc}
\usepackage{pgfplots}
\usepgfplotslibrary{fillbetween}
%\pgfplotsset{ % Define a common style, so we don't repeat ourselves
%	slopefield/.style={
%		%width=0.6\textwidth, % Overall width of the plot
%		axis x line=center,
%		axis y line=center,
%		%axis equal image, % Unit vectors for both axes have the same length
%		view={0}{90}, % We need to use "3D" plots, but we set the view so we look at them from straight up
%		xmin=-3, xmax=3.1, % Axis limits
%		ymin=-3, ymax=3.1,
%		domain=-3:3, y domain=-3:3, % Domain over which to evaluate the functions
%		xtick={-3,-2,-1,0,1,2,3}, ytick={-3,-2,-1,0,1,2,3}, % Tick marks
%		samples=22, % How many arrows?
%		cycle list={    % Plot styles
%			gray,
%			quiver={
%				u={1/({sqrt(1+(f(x,y)^2))})}, v={f(x,y)/({sqrt(1+(f(x,y)^2))})}, % End points of the arrows
%				scale arrows=0.075,
%				every arrow/.append style={
%				%	-latex % Arrow tip,
%				},
%			}\\
%		%	red, samples=31, smooth, thick, no markers, domain=0:1.1\\ % The plot style for the function
%		}
%	}
%}




%%
%% Enumerate
%%
\usepackage{enumitem}
\setenumerate[1]{label=\thesection.\arabic*}
\setenumerate[2]{label*=(\alph*)}